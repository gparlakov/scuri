import { <%= name %> } from './<%= normalizedName %>';

describe('<%= name %>', () => {
  it('it should', () => {
    // arrange
    // act
    const x = <%= name %>();
    // assert
    // expect(x).toEqual()
  });
});

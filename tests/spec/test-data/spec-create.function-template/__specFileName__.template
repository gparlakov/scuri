import { <%= name %> } from './<%= normalizedName %>';

describe('my function is <%= name %>', () => {
    it('it should', () => {
    // arrange
    // act
    const x = <%= name %>();
    // assert
    // expect(x).toEqual
    });
});

describe('existing spec', () => {
});
    function setup() {
        let a: string;
        const builder = {
            a,
            build() {
                return new T(a);
            }
        }
    }
